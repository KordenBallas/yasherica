# Product Requirements — Verified Briefs

This folder holds **product-owner requirement briefs**: feature specs written at a
product-owner level (user stories, what the feature does, how it should feel, acceptance
criteria, content/authoring rules, out-of-scope) **after** they have been discussed and
**verified** with the product owner. Each brief is the source of *intent* handed to the code
track for implementation.

## What these are (and aren't)

- These describe **intended** behavior, before it is built. They are the **exception** to the
  rest of `Assets/__Project/Docs/`, which documents systems **as implemented**.
- They are **not** the `/design` tree. `/design` holds forward-looking *design intent* (world,
  lore, narrative, crafting). A product-requirements brief is the concrete, verified hand-off
  for one change — the "what to build now", not the long-term design vision.
- They are **product-owner level, not technical**: no architecture, class names, or
  implementation steps. The code track derives the technical "how".

## Lifecycle

1. The product owner and Claude discuss a need and verify the requirement.
2. The agreed requirement is committed here as one brief (English, product-owner level).
3. The code track implements it; the **as-implemented** behavior is then documented in the
   relevant system doc per `Assets/__Project/CLAUDE.md §8`.
4. The brief stays as the original record of intent — it is not rewritten to match the code.

## Briefs

| Brief | Scope |
|---|---|
| [Encounter Dialogue UI](encounter-dialogue-ui.md) | Hades-style bottom-centre dialogue box (NPC portrait + name), word-by-word text reveal at reading speed, choice cards (quest / attack / exit) that surface after the line, quest card carries title + objective, author-marked keyword highlighting |
| [NPC Proximity Interaction](npc-proximity-interaction.md) | Replace land-on-platform triggering with proximity: walk into an NPC's interaction radius → **F** prompt → dialogue. NPC intent is **derived from facts** (quest-bearer `?` / hostile `!` / plain) — hostility emerges when the facts leave an NPC no quest to offer *and* it has an enemy behind it (auto-battle on entering the aggro radius), not from an authored flag. Always-visible markers; global radius config with a toggleable dev debug overlay |
| [World Content Density](world-content-density.md) | Stop the director flooding the world with quests. A **rare, breathing world**: four content kinds (empty/traversal, simple loot, ambient aggressive monsters, rare NPC quests) instead of quest-or-void; quests rare (~1 per ~10 platforms), monsters standalone and flat-difficulty, loot low-tier, empty space as deliberate budgeted breath. Designer tunes the whole mix from SO config; deterministic. Content-driven platform size/landscape is a separate later pass |
| [Crafting & Mutation — Socketed Blanks](crafting-mutation-socketed-blanks.md) | Replace "feed the cauldron and hope" with **building the organ**. Two item categories — **Part-Blanks** (skull/tail/wings/…: carry form + species/passport marker + sockets) vs **Artifacts** (function only, no species tag; raw→crafted quality gradient). Two stations: the **cauldron** fuses artifacts (no-failure, signature+emergent), a separate **operating table** sockets artifacts into a blank and **unseals** a **menu of variant mutations** (commit-on-unseal). Puzzle = readable direction + hidden menu + slot interaction (anti-recipe-table). Scarce **Blank Rack** = multi-track tension; traits hidden (archetype color + tier glow; cauldron-voice hints). **Retires the shipped feeding/digestion flow.** Card visuals + biome bias are separate briefs |
| [Combat — Hero Facing & Directional Abilities](combat-hero-facing.md) | Ability direction = the unit's **facing** ("eyes point where it fires"), not a per-ability aimed direction. **Global facing**: the whole queued volley fires along the current facing, and **turning re-points the entire queue + all its ghosts**. Facing is **free/unlimited** to change (chess-like); ring abilities unaffected; aim = **turn the hero** (reuse the direction input). Enemies carry a **committed facing** in their locked intent → reading facing = reading where the blow lands. Per-ability aiming intentionally dropped; multi-directional "turn-as-a-queued-step" deferred |
| [Combat — Enemy Intent Phase](combat-turn-intent-phase.md) | Turn structure prerequisite for the ghost telegraph. Replace round-robin decide-and-act-instantly with a Shogun-style **Plan → Act → Resolve** round: enemies **commit and reveal** a **locked** plan up front → the player plans/acts **seeing** it → enemy committed actions **fire as shown** (they whiff if you dodged; they don't re-target). Resolution order **player then enemies**; determinism preserved. AI scoring unchanged — only decide-timing + commitment change. Intent *visuals* live in the ghost-telegraph brief; initiative order + smarter AI deferred |
| [Combat — Ability Ghost Telegraph](combat-ability-ghost-telegraph.md) | Turn the "where" cell-highlight into a **"what it does"** preview. A translucent **ghost of an ability's full outcome** (caster action + affected-unit push/pull/damage, against the **current** board) plays once on queue-submit, then fades. Each unit — **player and enemy** — shows its **queued abilities as icons above it**; **hovering an icon replays that ability's ghost**, including enemy icons → **read enemy intent**. One mechanism unifying queue display + battlefield telegraph + enemy-intent. Queue-simulation preview + the enemy turn-flow that surfaces the plan are flagged dependencies |
| [Quest-Offer Card](quest-offer-card.md) | The quest **offer** as a rare-prize card (now that artifact tier ships). Ornate framed card vs plain chatter bubble; the job (title+summary, already shipped) + a **mystery reward slot** that **glows by tier**, is **tinted by belonging colour**, and shows the item **hidden** (silhouette/"?") — exact item never named, cauldron voice hints. **Inspect** reveals the quest detail, reward stays hidden. Several offers on one NPC sit together, same tier, differing by belonging. Symmetry: the mutation card **reveals**, the quest card **conceals**. Reward-roll, competing-offer logic, and card art are separate |
| [Mutation Choice Cards](mutation-choice-cards.md) | The **unseal variant menu** of Socketed Blanks, as a hand of cards. **Front visual-first**: body part pictured centre + ability icons beneath (no stat blocks). **Flip** (corner button) → the replaced part + its abilities (before→after on demand). **Hover an ability icon** → the **ability-preview popover** (description + a 3D hero **casting the ability's animation** on a battlefield, reusing D3 placeholders; idle pose for a passive) — a **shared grammar** with the Arena draft screen; **hover the part** → mini-model popover of the hero wearing the new part. Shared card grammar (glow = potency, colour = belonging); crafting traits stay hidden (only abilities shown); picking **commits**. Always-on in-world live preview + production VFX (P5-12) + quest-card visuals are separate |
| [Biome Visual Styles](biome-visual-styles.md) | Data-driven per-biome look: one appearance config per `LevelTheme` (Forest/Desert/Mountain/Cave, mirroring the biome-loot SO) = ground material, **palette key** (a muted key modulation of the master palette, inherits the render-look bible), light, and a **feature pool**. Features place on **whole hex cells**; each is **decorative or blocking** (blocking = cell unavailable, no cover/LoS) and can't drop a combat platform below its battlefield minimum. New biome/feature = data-only; deterministic. Biome = base layer; **site dressing** + **biome placement along the run** are separate |
| [Platform — Hex Surface & Shape](platform-hex-surface-and-shape.md) | Make a platform's top **surface = its combat grid** (one source of truth), instead of fitting a hex grid into a random blob at combat time. Surface **composed of whole hex cells**, tiling **muted in traversal / crisp in combat**; **organic decorative rim** beyond the last full cell (natural island silhouette); **per-content-kind size profiles** (Empty/Loot/Combat/NPC) with a **battlefield minimum** for combat platforms, replacing random sizing; biome features **occupy whole cells** (obstacles align). Biome *styling* + multi-platform Site footprints are separate briefs |
| [Arena Mode (Multiplayer) — MVP](arena-mode-mvp.md) | A **secondary combat mode** (Pillar 4 only — no narrative/mutation/crafting): a main menu with **Journey** (→ the current scene, unchanged; Hub deferred) and **Arena** (→ a networked scene). Arena = **FFA 2–4 players**, host/join by **direct connect** (no lobby/matchmaking), **one platform**, a **default hero** each. Same combat rules as PvE, but the round is **hidden simultaneous commit → simultaneous resolve** (the symmetric form of the queue; deterministic per seed, committed actions **whiff** if dodged, FFA conflicts resolve by a deterministic rule). Win = **last hero standing**. Builds on the existing Netcode-for-GameObjects scaffold. Deck/hero snapshots, character select, PvP balance, and a Hub scene are deferred |
| [World Backdrop, Elevation & Routed Path](world-backdrop-and-elevation.md) | The full **P5-2**: make the traversal field read as a **landscape you route through**, not a straight staged line, on three layout/visual axes — **routed path** (platforms weave laterally along a coherent bounded low-frequency curve = baseline wander + sparse feature arcs around a far-side midground **landmark**; monotonic screen-forward; **fixed isometric camera unchanged**; replaces today's unbounded random depth-drift), **elevation tiers** (deliberate height levels), and **world/biome backdrop + skyline** (a distant hazed horizon). Character per biome on the biome appearance config; deterministic. **Routing/read, never traversal** — gaps stay clean hops, path never forks. Path-following camera yaw, route↔real-backdrop coupling, and the decoration meshes are deferred |
| [World Sites & Landscape](world-sites-and-landscape.md) | Content shapes the world (builds on Content Density). Two axes **Biome × Site**; content-first — a settlement-scale beat pulls a **Site** (camp/village/city) or a **Landmark** (ruin/lair) into being, a **cluster of platforms** that reads as one place (connected islands + dressing), while most of the world stays **Wild**. Each site fills its footprint by a **capacity recipe** (anchor + weighted fill + connective) over one shared **base·flavor content vocabulary** (Empty/Loot/Combat/NPC × open flavors; quest & hostility derived). Built to stay **extensible/maintainable** (data-authored sites/flavors, family-default + override, additive attributes). Beta backlog + apex throne + landscape art deferred |
| [Part-Blank Loot Sources](blank-loot-sources.md) | Give **Part-Blanks** (body-part items) a real world source so the find→socket→unseal→install loop runs in a real run (today only a dev seed). **Two channels:** **enemy remains** (primary — kill a creature, occasionally loot the blank of one of its parts, on the combat/mutation channel, separate from quest rewards) + **Landmark finds/relics/chests** (rarer, stronger blanks in ruins/lairs — "direction + floor, not vending"). Light, tunable biome/kind **bias** (tendency never guarantee); deterministic. Socketing flow, quest-reward economy, and per-run dig meta-bias unchanged/deferred |
| [Combat — Enemy Move-Destination Telegraph](combat-move-destination-telegraph.md) | Draw an enemy's **committed move destination on the board** during the plan phase (today only a `»` glyph above the unit; destination not drawn — `combat-round-and-telegraph.md` §6). Completes the read-the-intent picture (step + facing + ability) for "fast chess". Facing stays on the model (on-hex `>` glyph **cut**); shows intended destination even if the move whiffs. Presentation only — no change to movement, AI, or turn order. Ability animations + full path line deferred |
| [Medallion Socket UI](medallion-socket-ui.md) | Re-present a socketed Part-Blank as a **medallion**: part centred, sockets as **gems around the rim**, the **rim a progress line** that fills as sockets fill and **closes** when ready — and only **then** offers the mutation (a **confirm-before-unseal** beat replacing today's auto-unseal on the last socket). Gives the "build the organ" loop a tactile read and a deliberate placement (fixes "shoved left of the cauldron"). Presentation + one interaction beat over the shipped socketing model; feeds the existing variant menu; traits stay hidden (archetype colour + tier glow). Partial-fill threshold + stomach backdrop separate |
| [Race Roster & Passport Model](race-roster-and-passport.md) | The starting **races as data** and the **passport** rule. Three races, one per biome, as three estates of the Order — **Ibex** (Mountain, nobility), **Lizard** (Desert, clergy/inquisition), **Fox** (Forest, underworld/foil) — each with a home biome, belonging colour, and signature marker parts; the **hero is kindless**. **Every body part carries a race tag** (or *kindless*), and a race **accepts you by how many of its parts you wear** — 0 outsider / 1 tolerated-freak / 2+ kin — exposed as a **per-race count/tier fact** the narrative gates on (no hard part-conflict; the trade-off is the slot budget). Content gate for **P0-1**. Marker meshes/body-plans, per-race NPC/quest/enemy content, exposure/betrayal, biome selection, and a 4th Cave race are separate/deferred |
| [Run Escalation](run-escalation.md) | Make a run **feel like it climbs** — the first **consumer** of the `run_escalation_tier` fact (published by biome-selection). **One mechanism, three effects:** every placeable content (story, monster) carries a **run-tier band** and the director filters the eligible pool by the current tier. **(1) Register** — stories band into **Backwater → Courts → Divine/Apex**; low-register content ages out as high-register enters ("epic, but vile"). **(2) Difficulty** — monsters carry the same band; a higher tier draws a **tougher pool** (bigger bodies / more parts / nastier kits from the same part system), **NOT a stat multiplier** — difficulty is *different creatures to read* (Pillar 4), never inflated HP. **(3) Density does NOT scale with tier** — the breathing world holds at every altitude; the local "a court is busier" read is owned by **Site-tier**, not run-tier. `min`-tier opens content upward, `max`-tier optional. Deterministic + data-authored (unbanded = eligible everywhere). Run apex/finale, the register content itself, per-tier stat scaling, and any tier UI are separate/deferred |
| [Biome Selection Along the Run](biome-selection-along-the-run.md) | Stop the run being stuck in hardcoded **Forest**. The biome **advances along the run** through an **authored, seeded, escalating** sequence: each biome has an **escalation tier** (backwater → approach-to-the-Order), the run **climbs tiers**, and within a tier the biome is **seeded-picked from a weighted pool** → runs **diverge** in which homelands they open (D18). Travel in **stretches** (not per-platform flicker); a biome switch swaps its **monsters, loot, look**, and — for a homeland — its **race**. **Cave stays out of rotation** (no race yet). Deterministic + data-authored (new/re-tiered biome = no code); the **current tier is published as a fact**. Difficulty/tone escalation (D19), the run apex, and a player-choice map are separate/deferred |
| [Body-Part Authoring](part-authoring.md) | The **authoring contract** for a body part so a designer **or AI** can produce a valid one alone: **slot**, **size/proportion budget**, **socket & sub-part conventions** (ears/eyes on a head's sockets), **skeleton family** (+ marking a part that **pulls its own skeleton**, per the independent body-plans model), and **rarity re-texture of one mesh** (common frog head green vs legendary set). Conform to the published bind-pose/bone-naming contract. Authoring rules only — the runtime skeleton-swap mechanics, the race roster, and the generation pipeline are separate (see `design/characters/model.md`, `needs-code.md`) |
| [Director — Threads, Continuity & Fact Horizon](director-threads-and-continuity.md) | The **P2-3** long pole (after the shipped actor/faction eligibility): make a run read as **one growing saga** and place its beats **in causal order**. A **thread** becomes a first-class managed entity (id + state), in two kinds — **ephemeral** (can **expire** silently past an authored lifespan → failed/expired indicator, no closing beat) and **arc** (long, expiry-exempt). Either kind **fails on fact-conflict** when the player makes its premise impossible (joined the Lizards → the "join the Foxes" thread fails; killed the king → the "befriend the king" arc fails) — the mutual-exclusion mechanism, again just a state change + indicator (**no closure-beat content**). A small **concurrency ceiling**; the director **advances** live threads over opening new ones. **Cross-window continuity**: a consequence beat is **held until its cause's fact is live** and never placed early; an already-offered/resolved/failed story is **never re-placed** (correctness over density). Also draws the **run-scoped vs meta-scoped fact boundary** now so save/load (P2-2) persists a clean partition. Deterministic. Spine lane (P3-1), escalation (P3-2), the cross-run meta store + file IO + consumers (P2-2/P3-3), and a player-facing saga view (near P1-11) are separate |
| [Camp Shady Offer](camp-shady-offer.md) | **P3-17.** What `NPC·quest-bearer` means as a **fill** beat instead of a settlement **anchor** — the **Camp's shady offer**. A Camp's reason to exist stays its **bandits** (anchor); a rare fill quest-bearer is an **outlaw's dirty job**, the world's **dark-currency** quest source (power/combat reward, cauldron purrs). "Shady" is **derived from the Camp's outlaw occupancy** (no new flavor); the offer is **passport-free** (outlaws take any beast → the one quest source open to a **kin-nowhere chimera**, per §9 Monster road); the giver is the **bandit boss fronting the camp** (talkable `?` + F, reachable without fighting first), his **crew the fight *behind* the deal** (attack card / betrayal), not an aggro wall. May be self-contained or the dark half of a cross-actor fork. Deterministic; no existing asset or content kind changes. Fork mechanics, reward-roll, offer-card UI, and the engine "how" are separate/consumed |
| [Arena — Part Draft & Tasted-Forms Catalog](arena-part-draft-and-catalog.md) | ✅ **Shipped 2026-07-06** (`arena-mode.md` §2.9; see CHANGELOG). **P4-5.** Arena's character layer, deciding the model **against the Hades frame** — replaces the MVP's default hero. Each match opens with an **in-match parts draft**: players **snake-draft individual body parts** off a **single shared board** (a part taken is **gone for others** that match — denial) and **assemble a monster**, then the unchanged MVP round rules run. Board = a **common floor** of baseline parts + a sample from the **union of participants' unlocked catalogs** (your Journey finds seed the table; the board is the same for everyone → fair). What carries from Journey = a **catalog of *parts*, not heroes**, unlocked **passively** by play (install-at-minimum; no currency/grind), **meta-persistent**, **read-only for Arena** (widens the board only; no in-match power; never feeds back into Journey). Snapshot "deck of heroes" **rejected** (fiction: *the cauldron remembers tasted forms*, not a stable of champions; balance: no PvE contamination). Budget guard **relaxed but fenced** — sidegrade parts + common floor keep it variety-not-power. Frame-changing parts in Arena, draft variants, and all tuning numbers deferred |
| [Save / Continue a Run + Cross-Run Memory](save-continue-run.md) | ✅ **Shipped 2026-07-05** (`save-persistence.md`; see CHANGELOG). **P2-2** — the last P2 long pole. Two halves of the Hades frame. **(1) Continue:** one invisible **autosave**, **no slots, no save-scum** — quit mid-run and relaunch resumes the run **whole** (same world ahead + seed/generator position, hero body/frame, blanks/artifacts/currency, quest log, facts, live threads, met-actors), felt as one coherent resume; **death consumes the save** (a dead run can't be reloaded). Savepoints are **platform-clean** — quitting mid-fight/dialogue resumes at that platform's **clean start** (the encounter re-begins; suspended = non-savepoint, W3-1), earlier progress intact. **(2) Cross-run memory:** a **separate always-on store + file IO** that survives death, persisting the **meta-scoped facts** (the run/meta partition already drawn in P2-3) as its first client, proven by **one demo persistent fact** observable in a following run. **Fail-safe:** run-save and memory are independently recoverable — a corrupt run-save discards to a fresh run with memory intact; a corrupt memory degrades to empty world-memory; neither crashes; stale saves never hard-crash. The meta **consumers** (mirror-lore, cauldron memory, spine cursor — P3-3), the Arena catalog (P4-5), manual slots, mid-encounter fidelity, hub/meta-progression systems, and any save UI beyond a Continue button are separate/deferred |
| [Humanoid Bandit Camp](bandit-camp-humanoids.md) | **Track D · 1/3.** Retire the placeholder **capsule** enemies: render bandits (and NPCs) with the **shared humanoid model** — the same one the hero uses — told apart for the demo **by colour** (peaceful **green**, bandit **boss deep maroon**, **crew lighter maroon**), and kept in that look **in combat too**. A camp = a **boss + a director-sized crew**; the **boss may carry a quest or not**, the **crew never does**. The boss has a **larger engagement circle** than a normal NPC, **active only on his own platform**; crossing it **opens dialogue** (has a job) or **starts the camp fight** (no job), and his **crew joins that one fight** — no independent tripwires. **Landing on a camp platform starts nothing.** Deterministic, data-authored. Final faction art, the shady-offer content, and the in-combat feel are separate |
| [Combat — Initiative, Turn-Order Queue & Facing Input](combat-initiative-and-turn-queue.md) | **Track D · 2/3.** Give the fight a legible order and a clear control loop. **Initiative:** the **one who started the fight acts first** — player if the player attacked, enemy if the fight came out of a dialogue **or** a no-dialogue ambush (replaces the fixed player-then-enemies opening). **Turn-order strip:** a **horizontal** read-out of who acts and in what order, in the **top-right** (demo placement), updating as the round runs. **Aim/fire input:** **hold Enter** to turn the hero toward the **mouse cursor** (re-points the whole queued volley, per shipped global-facing), **release Enter to execute** along the final facing. Presentation + turn-flow only; abilities unchanged. Speed-based initiative, multi-round policy, final HUD, and remapping deferred |
| [Combat — Ability Animation, Animated Ghost & Enemy Action Read](combat-ability-animation.md) | **Track D · 3/3.** Actions become legible over time. Every ability gets an **animation** — **authored by the code track as a placeholder** (like the body-plans test frames), **spanning the ability's whole affected area**. It plays **translucently** when the ability is **queued** (the ghost telegraph, now moving instead of a static clone) or its icon is **hovered** (incl. enemy intents), and **for real** when it **executes** — **including on the enemy's turn**, so an enemy action **plays out at a readable pace, not instantly**. An enemy's committed **move** shows a **direction arrow** on the board (replacing the `»` glyph). Resolves the deferred ghost-caster-animation follow-up; presentation only, outcomes/turn-order unchanged. Production art, per-ability choreography, queue-simulation, and full path lines deferred |
| [Independent Body-Plans & Skeleton-Swap Runtime](body-plan-skeleton-swap.md) | ✅ **Shipped 2026-07-04** (`character-system.md` R19–R24; see CHANGELOG). The **runtime** half of independent body-plans (**P2-1**, supersedes `character-system.md` R10/R3). Most parts ride one **base frame** and swap cheaply as today; a rare **skeleton-changing part** **pulls its own body plan** (a legless serpent, a quadruped) and the body **re-forms** around it — the honest, felt cost of reshaping your frame. Which frame governs is decided by authored **priority**, **deterministically**; parts with **no attach point** on the new frame are **shed**. UX: **confirm-before-change** showing which parts come off (decline = no change), and **shed parts return to inventory** (not destroyed). First scope = the full machinery + base frame + **two** demonstrator plans, authored by the **code track as placeholder test frames**: a **legless serpent** (removes attach points → sheds both legs) and a **spider-legged** frame (adds attach points → sheds the base legs), together testing both directions and making the priority rule falsifiable (serpent+spider equipped → higher priority governs). The three starting races are all base-frame (need no special skeletons); passport reads a smaller frame as simply fewer markers (no special rule). Production frame meshes (P5-6), further plans, rarity re-texture, and the generation pipeline are separate |
| [Dressing-Kit Binding & Swap](dressing-kit-binding-and-swap.md) | ✅ **Shipped 2026-07-06** (`environment-dressing.md`; see CHANGELOG). **Track E · 1/4** (foundational). The one new design decision of the Environment Dressing track: a **dressing kit** = a named, **data-only** bundle of decoration-asset references that a seam (biome feature pool / site dressing theme / world backdrop) binds **as a whole**. **Swap = repoint one field** — demo (store-pack) kits now, production kits later, no code/scene edits. **Clean-swap invariant:** no store-pack asset is hard-referenced from a scene, prefab, or code (only from a kit) → a production build drops the demo packs cleanly. **Tone treatment (non-negotiable):** every kit is pushed down the value/saturation hierarchy under the biome key (muted/hazed) so the hero/enemies/loot still pop. Placement/determinism unchanged (a kit only supplies *which* assets). The demo content, production assets, and the shader spike are separate |
| [Biome Decoration Kits (Demo)](biome-decoration-kits-demo.md) | ✅ **Shipped 2026-07-06** (`environment-dressing.md`; see CHANGELOG). **Track E · 2/4.** First real content for the biome **feature pool + ground** (`biome-visual-styles`): **Desert** ← *Low-Poly Desert Environment Pack* (sand ground + rocks/cacti/shrubs), **Forest** ← *RPG Poly Pack - Lite* (grass ground + trees/bushes/rocks/tufts), each a demo **biome-feature kit** with per-feature density + decorative\|blocking. Bound via the kit contract; muted (not the packs' native saturation); placement stays the feature pool's (deterministic, whole cells, battlefield minimum). Mountain/Cave, production assets, and new terrain rules are separate/deferred |
| [Site & Camp Dressing Kits (Demo)](site-camp-dressing-kits-demo.md) | ✅ **Shipped 2026-07-06** (`environment-dressing.md`; see CHANGELOG). **Track E · 3/4.** First real dressing for the **site dressing-theme** seam, all from the *RPG Poly Pack*: a **Settlement kit** (houses/skyline/ground/gate — Village & City share it) and a **Camp kit** (awning/sacks/campfire/barrels + packed-dirt). The reads-as-one-place method (aligned skyline / shared ground / gradient / gate, **dressing not geometry**, gaps stay clean hops) is inherited unchanged; same kit **recolours per biome**. **Orthogonal to the camp *enemies*** — bandit boss/crew + shady offer stay Track D + P3-17. Landmark (ruin/lair) kits, production assets, and prop animation deferred |
| [Decoration Footprint & Edge-Fit](decoration-footprint-and-edge-fit.md) | ✅ **Shipped 2026-07-06** (`environment-dressing.md` R17–R19/R16a; see CHANGELOG). **Track E · polish.** Stop wide decorations spilling past the platform edge. Placement is point-based today (prop dropped on a cell/rim point, no notion of its width) → wide props overhang the void. **Account for a decoration's size:** each prop carries a rough **footprint** (kind-default + optional per-prop override, light authoring), placement keeps a **keep-clear margin from the true edge** and **nudges inward rather than dropping** (density/clustered look preserved); cluster members obey the same margin. **Keeps the deliberate rim-framing overhang** (tall trees/crags leaning canopy over the edge) but as an **opt-in "may overhang" mark on a few props only** — every grounded prop stays on the platform; base always on a foothold. Deterministic (footprint = kit data, not a new draw); blocking/lane/battlefield-minimum/tone/density unchanged. Prop-vs-prop spacing, mesh-derived bounds, site dressing, and the E4 backdrop are separate |
| [World Backdrop Fill (Demo)](world-backdrop-fill-demo.md) | ✅ **Shipped 2026-07-06** (`environment-dressing.md` §1.5/R20–R22; see CHANGELOG). **Track E · 4/4.** Fill the space **behind the platforms now** — the distant low-poly landscape/horizon (the Windblown read), populating the designed-but-mesh-less **world backdrop** layer. **Technique decided: real 3D low-poly scatter** (distant dunes/mesas/hills/tree-clumps), **not** a flat billboard — reuses pack meshes, real parallax depth. A demo **backdrop kit per biome** (Desert dunes/mesas/cacti; Forest hills/treeline/rocks), heavily **desaturated/hazed**, low density, the **quietest layer**. **Background, never traversal** — behind the field, not walkable, never occludes gameplay; gaps stay clean hops; deterministic. Mountain/Cave, horizon escalation, route↔backdrop coupling, camera yaw, sky animation, and production assets deferred |
| [Cauldron View — Spatial Spine & Zone Rule](cauldron-view-spatial-spine.md) | **Track F · 1/3** (foundational; gates F2 & F3, re-homes P1-3). A **legible vertical three-zone layout** for the pot view — **crafting zone (top) · brew/cauldron (centre) · a horizontal ribbon of Part-Blank medallions (bottom)** — reading as one interior (incubating organs → brewing reagents → crafting/result). Plus **one rule: bubble = "in the brew"** — an artifact wears a bubble **only while suspended in the cauldron liquid**; staged, result, and socketed artifacts are **bare** (so the bubble doubles as a state signal, and drag-across-the-waterline visibly adds/removes it). Re-homes the socketing rack from **left-of-cauldron** to the **under-pot ribbon** (`medallion-socket-ui.md` FR7 placement); ribbon/crafting zone anchor to the frame, not the pot. Presentation + one rule over shipped inventory/crafting/socketing — no behaviour change. Liquid/fullness (F2), stable layout/physics (F3), the medallion internals (P1-3), the stomach backdrop (P5-5), and the prettier mesh are separate |
| [Cauldron View — Liquid You Can Feel & Fullness](cauldron-liquid-and-fullness.md) | **Track F · 2/3.** Make the brew **read as real liquid** and the **fill level** the one cue for **how full the inventory is**. A **stylised low-poly translucent surface** with a **crisp waterline/meniscus** (keeps the boil/glow), and **bubbles that pierce the surface** — each artifact sits **half-submerged**, cut by the waterline (the strongest "it's *in* the liquid" read), submerged part tinted, emerged part clear/clickable. **Fullness drives the liquid level** (few = shallow pool, many = brimming, between a min/max), **snapshotted on open** so it holds steady through a session. **Cauldron model is a fixed size — never resized in code, no discrete size swaps** (owner-locked; the prior "model grows" idea dropped). Stays flat-low-poly / no-outline (`render-look.md` §1), a reserved accent that never out-shouts the artifacts. Read-only presentation — no logic change. Per-bubble placement/physics (F3), the prettier mesh, and the stomach backdrop (P5-5) are separate |
| [Arena — Per-Client Seat Camera & Oriented Backdrop](arena-seat-camera-and-backdrop.md) | **Track G · G1.** Each Arena client views the **shared** board **from behind its own hero** (near-edge **seat**), so every player reads the board from their own familiar side — the *feel* of Journey, not a literal copy. **Static** for the match (owner: static seat, not a chase-cam — a hex board needs a stable frame to read the queue, Pillar 4); 2–4 players each get a distinct near-edge seat around one platform. The world **backdrop turns *with* the seat** on **one local view rig** (owner: rotate-with-seat, not a 360° surround; arena/platform/units unmoved), reusing the existing hazed backdrop art. **Purely client-side** — hex coords, positions, committed queues, and deterministic resolution stay identical on every client (same board, rotated). Board interaction (hover/ghost/telegraph) must stay correct under the rotated view — the same seam as the **G3** hex-highlight bug. **Arena only** (Journey unchanged). Follow-cam, 360° surround, zoom/pan, and spectator polish deferred |
| [Arena — Developer Console (HP + Turn Log)](arena-dev-console.md) | **Track G · G2.** A **dev-only** overlay in Arena (toggle key, absent in release, not player-facing): **current player HP** for every combatant (owner: current only, no deltas) + a **turn log** that instruments the hidden-simultaneous-commit → simultaneous-resolve round — **both** the per-round **committed-queue reveal** per player **and** the **step-by-step resolution order** with **whiff / conflict** annotations, as a running history. The instrument for confirming the **deterministic** resolve (`arena-mode-mvp.md` §9–12). Extends the general dev-tools surface (**P6-6/P6-7**). Player-facing combat log, mutating dev controls, configurable keybind, and log-to-file deferred |
| [Director — Spine Reserved Lane & Reveal Cap](director-spine-reveal-lane.md) | **P3-1** (Track I). The placement **mechanism** for the curated reveal spine (the mirror antagonist, the cauldron cycle — `lore-pacing.md`). A **reserved lane** places spine beats **first, outside the density weight budget** (like the combat minimum), throttled by a **per-run reveal cap** (authored, default ≤1–2). The spine is a **pool of precondition-gated beats, NOT a linear queue** — a beat is eligible when its preconditions + a **soft floor** hold and it hasn't been revealed; the puzzle **assembles in any order**, and "never too early" is enforced **per-beat** (deep preconditions + floor on the heavy reveals), not by sequence. **Decoupled from win/lose** (a lost run advances it too). Gates are **plain facts** + a **meta run-counter** (no new milestone subsystem). Deterministic. The cross-run persistence of the revealed set (spine cursor), mirror-lore, cauldron memory, the reveal content itself, escalation, and the meta store are separate (P3-3 / P2-2 / P3-2) |
| [Rolled Quest Reward — Tier + Belonging](quest-reward-rolled.md) | **P1-5 + P0-3·b** (Track H). Stop a quest handing a **literal item**: it declares **(tier, belonging, payload kind)** and the world **rolls** the reward on completion (honest glow, non-catalog). Reconciles Socketed Blanks — **archetype/race left artifacts**, so payload = **a Part-Blank** (belonging = its **race**, via `RaceDefinition._belongingColor`) **or an Artifact** (function-only; belonging = a coarse **function family**, no race), **declared per quest** (owner call). Access is still paid in facts/doors too; the roll is a **bias, never a guaranteed piece**. The offer-card visual, competing-offer placement, non-item sinks (P1-12, deferred), and roll tables are separate |
| [Attack Card — the Monster Verb](attack-card-monster-verb.md) | **P1-7** (Track H). Make **attacking a talkable NPC** a first-class verb with real consequences (not the card visual — that shipped): present it **only on eligible NPCs** (or NPC **self-initiation**), and on the kill **close that actor's thread** (P2-3 foreclosure = the in-fiction price), **drop corpse-loot on the separate combat/mutation channel** (never balance the fork on it), **write Conquest/path facts** (lean the run, close kin-doors), and **fire a cauldron bark slot**. One verb, two entry points (card pick / self-initiation), same consequences. Bark content, the competing-fork placement, and combat internals are separate |
| [Multiple & Competing Offers](multiple-and-competing-offers.md) | **P1-9 + P1-8** (Track H). The **two offer shapes**, kept distinct: **Shape A** — several resolution cards on **one NPC at one moment** (several ways to solve one trouble); **Shape B** — a **mutually-exclusive fork across two actors, separated in time** (commit to A now, opposed offer B a couple platforms later on a shared thread, enforced by **P2-3 fact-conflict thread-fail**, never side-by-side). Both are **same tier / different currency** (power vs access) → no dominant pick; never balanced on tier or corpse-loot. The rolled reward, the card visual, the attack verb, bark content, and a player-facing thread view are separate |
| [Cauldron-Voice Barks](cauldron-voice-barks.md) | **P1-10** (Track H, **Hybrid**). The cauldron's **reactive, live bark channel** (no meter) — short lines at authored **slots** (a monstrous mutation offered · the dark offer / attack verb · restraint · a **socketing trend** via the existing `ISocketingTrendSource`), tone shifting with the **path lean** (bolder indulgent / sourer restrained) off existing facts. Lines are **data-authored** (designer writes them, code builds the seam). Deliberately **separate** from the curated **reveal beats** about past hosts (those ride P3-1's spine lane, `director-meta-consumers.md`). Audio, hub lines, and the full pool are deferred |
| [Quest Log & Saga Readout](quest-log-and-saga.md) | **P1-11** (Track H). A **read-only** window: a quest log (**Active / Completed / Failed-Expired** + objectives/giver) that **also** groups quests by **thread** into a **saga readout** (the view P2-3 deferred here) — a resolved moral fork reads as saga (taken arc + **foreclosed** opposing thread), a failed thread shows failed/expired (indicator only). Reward stays **tier + belonging, exact item hidden** (offer-card rule holds). Derives from the shipped progression record + threads; mutates nothing. UI art, non-item rewards (P1-12), cross-run history (P3-3), and filtering are separate |
| [Director — Cross-Run Meta Consumers](director-meta-consumers.md) | **P3-3** (Track I). The **reading** side of the meta horizon (D20) — three consumers that turn **persisted meta facts** (P2-2 shipped `meta.json`) into in-fiction reveals. **(1) Spine cursor** — the already-revealed set **persists across runs** (meta), so the spine **advances** run over run and never repeats, surviving death. **(2) Mirror-lore echoes** — a **bounded, authored** table of tyrant-lore variants **gated on persisted key choices** (path lean, landmark deeds), so the antagonist mirrors your history — **not** bespoke per-choice, **not** generated. **(3) Cauldron memory of past hosts** — the voice's asides as **meta-gated reveal-beats** riding **P3-1's spine lane** (capped, rush-proof), **NOT** the real-time tempter-bark channel (P1-10, Track H). In-fiction only (no codex); graceful degrade on empty/corrupt memory; deterministic. The lane mechanism (P3-1), the store/partition (P2-2/P2-3), the bark channel (P1-10), and the reveal content are separate |
| [Arena — Parts Draft Screen (UI & Presentation)](arena-draft-ui.md) | ✅ **Shipped 2026-07-06** (`arena-mode.md` §2.9 + `ability-preview-popover.md`; see CHANGELOG). **Track G · G4.** The **screen** for the P4-5 parts draft — presentation only, no new model. Shared board **laid out by slot**, **each part's abilities shown openly** (owner call: Arena is pure tactics, so the campaign's hidden-trait economy does **not** apply here), unmistakable **whose-pick** indicator + legible **snake order**, **remote picks animate**, **denial** removes a taken part for everyone. The local player's **hero assembles as a live 3D model** + a **name+parts** readout; **opponents** show as **name + drafted parts** (owner call: cleaner than opponent 3D models). **Clickable 3D part models** open part/ability info; **hovering an ability icon** opens a shared **ability-preview popover** (description + a 3D hero **casting the ability's animation**, reusing D3 placeholders) — the same popover the mutation cards use. **Soft per-pick timer + auto-pick** so a networked FFA draft never hangs; a **"your monster" beat** then hands to the unchanged MVP round. Reads P4-5 state, changes nothing (determinism preserved). Draft model/board composition/tuning, frame-changing parts, opponent 3D models, production VFX (P5-12), and the in-fight seat camera (G1) are separate |
| [The Hub — Staging, Start-of-Run Choices & Death Return](hub-staging-and-launch.md) | **Track O · O1.** Stands up the **Junkyard Hub** as a real scene: **Journey → the Hub** (not straight to a run) + the **death-return** reform point. Two **independent** opening choices realize the canonical "direction + floor" dig — **(1)** pick **one of three starting organs** (each a ready installed part with a **visible active ability** + a **race marker** → hero becomes a **1-marker tolerated freak** of that race; one per Ibex/Lizard/Fox), **(2)** pick the **starting biome** (part-race need **not** match the biome → match=tolerated-at-home vs cross=marked-outsider, a divergence generator). **Refines** shipped biome-selection (P0-2) for the **entry point only** — the climb after entry stays the seeded pool. Cauldron voice comments (data-authored). Death returns to the Hub; run image consumed (permadeath, P2-2), **meta memory persists**; picks are run-scoped. Hub meta-progression, recurring cast, artifact digs / N>3, and a full route-choice map are deferred |
| [The Hub Is Just Another Biome — One Platform, the World's Own Camera & Controls](hub-as-a-normal-platform.md) | **Track O · O1 rework** (corrective). The Hub was built as a **parallel world** — its own platform assembly, camera, and hero movement — and feels wrong. Fix the divergence: **the Hub is just a normal biome with exactly one platform**, indistinguishable from a normal world platform in **camera angle/framing/follow** and **hero movement/controls** (reuse the world's own camera + locomotion + platform; **no bespoke Hub camera/movement/platform** left). Only **style + content** differ — a data-authored **hub-biome look**, **one keeper NPC** slightly **left of center from the shared camera's view** (**[F] → the three starting-part cards**), and **three small-radius portals** each **labelled with its biome name** (**[F] shows the name and launches the run into that biome**); no combat/enemies/streaming. Supersedes only the **scene-assembly/camera/control** parts of `hub-staging-and-launch.md` — the staging **domain** (part offer, independent biome pick, cauldron voice, death-return) is correct and **unchanged** |
| [Cross-Device Input Foundation](cross-device-input-foundation.md) | ✅ **Shipped 2026-07-07** (new system doc `input-foundation.md`; see CHANGELOG). Make the game playable end-to-end on **keyboard+mouse, gamepad, and touch** — the first, most architectural slice of going multiplatform. Play through a fixed set of named **actions** (Move/Interact/Confirm/Cancel/Navigate/Aim/Fire), each **bound on all three sources**, with the **active source detected live** so every on-screen prompt shows the right cue (**F** vs controller button vs touch) and flips instantly on device switch (supersedes the "controller glyphs out of scope" notes in `npc-proximity`/`encounter-dialogue-ui`). Deliberately the **input + prompt plumbing, not the screen redesigns**: pointer-heavy screens (cauldron drag, hover, mutation cards) already work under **touch**, but their **gamepad** interaction is genuine design and is **flagged, not faked**, for its own brief. No core action exclusive to one source; dev overlay shows the active source. Rebinding UI, mobile perf/safe-area, console SDK/certification, and haptics/gyro are separate tracks |
| [Combat Status Effects](combat-status-effects.md) | ✅ **Shipped 2026-07-07** (new system doc `combat-status-effects.md`; see CHANGELOG). **Track S (S1+S2+S3).** Turn combat's instant damage into a game with a **persistent condition layer**: a **status** = a named condition on a unit that **persists across turns, ticks, and expires** — **data-authored** (a new status is an asset, never code), **legible on the board** (on-unit glyph + remaining turns; the card's effect icon is the **same glyph**), **deterministic / Arena-lockstep-safe** (one fixed resolve point). Starter kinds: **DoT** (burn/poison/bleed) · **control** (stun/root/slow) · **stat-modifier**; per-status **stack rule** (refresh default / stack-to-cap / ignore); application rides existing ability targeting. **FR10:** statuses and part passives share **one modifier model** (a passive = the duration-less case). Combos (S4), rich dispel, new targeting, VFX, and status-aware AI are out of scope |
| [Architecture & Extensibility Audit](architecture-and-extensibility-audit.md) | **Track W.** A read-only whole-project **review** (not an implementation) — Fable reads the codebase + data systems and measures them against `CLAUDE.md`, producing a prioritised findings backlog. Split into scoped passes to fit context: **W1** data-driven/SO extensibility (add-one-instance touch-count · hard-coded content variety · SOs-with-logic), **W2** architecture, coupling **& duplication** (layering/DI breaks · God-objects · *same-job-done-twice* parallel implementations like two scene-specific platform builders, ranked by blast-radius), **W3** dead/unreachable code (enumerate → clear against Unity liveness channels; **hybrid** — cheap sweep + careful adjudication, since false-positive deletions are the dangerous kind), **W4** performance **hypotheses** (needs-profiling shortlist; no verdicts), **W5** docs↔implementation drift, **W6** synthesis → deduped, track-shaped refactor plan. Every finding cites `file:line`, ties to a rule/cost, carries P0/P1/P2 + refactor-cost + disposition, and never re-files known debt. Changes nothing; reports land in `Docs/audits/`. Applying any approved fix is a separate normally-scoped task |
| [Meta-Progression Spine — Unlock the Vocabulary](meta-progression-spine.md) | ✅ **Shipped 2026-07-07** (new system doc `meta-progression.md`; see CHANGELOG). **Track R** (meta half only; the in-run currency economy is a separate companion brief). The reiterability engine on the **Isaac model, not Hades**: cross-run meta **unlocks content into the possibility pool + biases the odds toward a pursued direction**, and **never** hands durable power / permanent stats / a guaranteed piece (keeps Pillar 1, the vending-machine rejection, anti-grind, permadeath). The unlock atom = a **grammar token** (a *multiplicand* — new blank form/artifact/trait/damage-type/tier-ceiling/recipe/exotic pool), so one unlock **multiplies** the combinatorial space (volume can exceed Isaac's, cheaply). **Base grammar fully open from run 1** (depth is in-run, Pillar 3; the meta widens **edges/axes** only — never the base multiplicands, the early-starvation failure mode). Earned by **deeds/passively** (the Arena-catalog precedent; the persisted **tasted-forms catalog** is the primary channel — "you are what you eat" *is* the unlock), often sharing the reveal-spine milestones. The deferred **junkyard dig meta-bias** realised — raise odds/floor, **hard cap < 100%** (clamped, never a guarantee), with **pool-dilution** as a free guardrail. **Generous-gating** as the starting stance (run 100 still opens, run 10 already deep — a front-loaded, stratified runway), made a **coefficient not architecture** so min↔generous slides in playtest. Control = **per-token fields** (gating mark/deed/weight) + **one master tuning SO** (pacing · deed weights · bias + ceiling · dig shape · dilution). Reads/writes the shipped P2-2 `meta.json` over the O1 tasted-pool dig. The **in-run currency** (coins/shops/P1-12 sinks), **devil-deal/temptation-as-economy** (parked), the **Track T bestiary readout**, world-loot rebalancing, Track Y/apex consumers, and the token content itself are separate/deferred |
| [Heat — Player-Chosen Difficulty](heat-ascension-difficulty.md) | **Track Y** (Fable; **hard dep: R built first**). Optional, in-fiction **player-chosen difficulty** — the third reiterability axis (D19 auto-climbs a run · R grows the possibility space · **Y = the player *chooses* to climb harder for accelerated R reward**; the throttle that keeps R's runway alive at run 100). Framed as the **cauldron's dare**, set at the **hub** each run. Forks confirmed: **(1) rules, not numbers** — every modifier is a rule/pool change that demands better play (enemies act first · one fewer body slot · raised creature-pool floor · greedier unseal menu · less healing · tighter aim window · stricter passport), **never** stat-inflation (the grind D19 refused); **(2) feeds R, no reward of its own** — higher total Heat **Heat-gates exotic tokens**, **accelerates R's unlock pacing**, and **raises the dig-bias floor**, always under R's **clamped never-guarantee ceiling**; no separate currency/prestige/cosmetic; **(3) a small modular pact** (menu of ranked modifiers, total Heat = sum, reward needs a minimum), not a linear ladder — runs diverge by pact too (Pillar 1); **(4) cauldron voice · hub · free every run (up or down, zero allowed) + a persisted high-water mark** (mastery signal; **not** the parked corruption-meter — nothing accumulates on its own). Deterministic; current pact run-scoped (consumed on death), best-Heat meta; **Arena untouched**. The concrete modifier list/values, a Heat currency/ladder/ratchet, stat-multiplier difficulty, and gating the apex/ending are cut/deferred |
| [Medallion — Heat Slot-Reduction in View](medallion-heat-slot-reduction.md) | **Bug-fix / Track Y follow-up.** When a Heat modifier removes medallion slots, the View currently shows phantom gems that appear available but don't accept drops and miscounts rim progress. Fix: **sealed slots are rendered as a distinct locked/burned state**, not as empty gems; **rim tracks only the Heat-adjusted count**; an artifact occupying a now-sealed slot is **ejected to inventory** (never silently lost). No visual change at Heat 0 |
| [In-Run Currency & Shops](in-run-currency-and-shops.md) | **Track R — the run-economy companion** to the meta spine (orthogonal: meta unlocks a *vocabulary* by deeds with no currency; this is a *within-run wallet* you earn, spend, and **lose on death**). Defines the **Currency** half of **P1-12** and backs the camp shady-offer's "power/combat currency". Forks resolved-to-recommendation (confirm): **one neutral coin** (no second/dark wallet — the moral fork's "different currency, not more" stays on facts/parts); **you buy *ingredients*, not power** (an **in-world vendor** sells artifacts/primitives/rare blanks by tier → still craft it yourself, Pillar 3; never a finished strong organ; meta-gated goods absent until unlocked); **coins come from the world** (loot caches + a small ambient-combat trickle separate from corpse-loot + selling) **not quests** (keeps rewards on parts/access); **in-world merchant beat, never a hub shop** (currency dies with the run). Opus/Sonnet (not Fable). Second/dark currency + devil-deal sinks, XP/Ability sinks, a run→meta donation tail, and power-buying services are parked/deferred |
| [Cauldron View — Stable Brew Layout & Event Physics](cauldron-brew-layout-and-physics.md) | **Track F · 3/3.** Stop the brew **reshuffling on every touch** and make artifacts **react physically** to drop-in/removal. **Hybrid: stable places + event physics** (owner-locked) — each artifact holds a **stable spot for as long as it stays in the pot** (adding takes a free spot, removing frees one, **no global reshuffle**; untouched bubbles never move), while physicality lives on **events** (a dropped artifact **splashes in and nudges neighbours**, which **settle back** — a short reaction, not perpetual jostling). The *layout* stays deterministic/testable; the *reaction* is view-layer juice resolving to the deterministic rest spots. Plus **continuous result flow**: while a fusion result hovers, clicking a pot artifact **auto-commits the result into the brew** and stages the new pick — **replaces today's "new selections rejected until collected"** (`inventory-subsystem.md` R19). Non-overlap invariant holds; outcomes unchanged. The surface-piercing (F2), the layout/spine (F1), and full continuous simulation (deliberately out) are separate |
